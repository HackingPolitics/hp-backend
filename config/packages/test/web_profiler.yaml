web_profiler:
    toolbar: false
    intercept_redirects: false

framework:
    profiler:
        enabled: true # must be enabled, at least to prevent "A client must have Mailer enabled to make email assertions."
        collect: false
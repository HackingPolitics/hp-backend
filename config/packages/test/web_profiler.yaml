web_profiler:
    toolbar: false
    intercept_redirects: false

framework:
    profiler:
        # must be enabled, at least to prevent "A client must have Mailer
        # enabled to make email assertions." and also to use the TraceableMessageBus
        # which allows inspecting dispatched messages for tests
        enabled: true

        collect: false